apiVersion: v1
kind: Service
metadata:
  name: {{artifact}}-service
  namespace: {{artifact}}
  labels:
    app: {{artifact}}
spec:
  type: ClusterIP
  selector:
    app: {{artifact}}
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{port}}
      name: http