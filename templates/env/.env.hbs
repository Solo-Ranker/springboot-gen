# ─────────────────────────────────────────────────────────
# {{project_name}} — generated by SpringbootGen
# Copy this file to .env and fill in real values.
# NEVER commit .env to source control.
# ─────────────────────────────────────────────────────────

{{#if sections.application}}
    # ── Application ────────────────────────────────────────
    {{#each sections.application}}{{@key}}={{this}}
    {{/each}}
{{/if}}
{{#if sections.database}}
    # ── Database ────────────────────────────────────────
    {{#each sections.database}}{{@key}}={{this}}
    {{/each}}
{{/if}}
{{#if sections.redis}}
    # ── Redis ────────────────────────────────────────
    {{#each sections.redis}}{{@key}}={{this}}
    {{/each}}
{{/if}}
{{#if sections.kafka}}
    # ── Kafka ────────────────────────────────────────
    {{#each sections.kafka}}{{@key}}={{this}}
    {{/each}}
{{/if}}
{{#if sections.mongodb}}
    # ── MongoDB ────────────────────────────────────────
    {{#each sections.mongodb}}{{@key}}={{this}}
    {{/each}}
{{/if}}
{{#if sections.security}}
    # ── Security ────────────────────────────────────────
    {{#each sections.security}}{{@key}}={{this}}
    {{/each}}
{{/if}}
{{#if sections.s3}}
    # ── S3 ────────────────────────────────────────
    {{#each sections.s3}}{{@key}}={{this}}
    {{/each}}
{{/if}}
{{#if sections.mail}}
    # ── Mail ────────────────────────────────────────
    {{#each sections.mail}}{{@key}}={{this}}
    {{/each}}
{{/if}}
{{#if sections.tracing}}
    # ── Tracing ────────────────────────────────────────
    {{#each sections.tracing}}{{@key}}={{this}}
    {{/each}}
{{/if}}
{{#if sections.elasticsearch}}
    # ── Elasticsearch ────────────────────────────────────────
    {{#each sections.elasticsearch}}{{@key}}={{this}}
    {{/each}}
{{/if}}
{{#if sections.websocket}}
    # ── WebSocket ────────────────────────────────────────
    {{#each sections.websocket}}{{@key}}={{this}}
    {{/each}}
{{/if}}
{{#if sections.other}}
    # ── Other ────────────────────────────────────────
    {{#each sections.other}}{{@key}}={{this}}
    {{/each}}
{{/if}}